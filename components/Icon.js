const Icon = ({name})=>{
    return (
        <div className={"material"}>
            <span className={"material-symbols-rounded"}>
                {name}
            </span>
        </div>
    )
}

export default Icon